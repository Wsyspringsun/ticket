<!DOCTYPE html>
<html>
  <head>
	  <%include ../head.ejs%>
  </head>
 <body>
    <%include ../nav.ejs%>
    <div id="example"></div>
    <div class="jumbotron">
      <div class="container">
        <form style="margin: 40px 0 0 0;" role="form">
          <input type="text" placeholder="关键字" style="float:left;" class="form-control">
          <button type="submit" class="btn">搜索</button>
        </form>
      </div>
    </div>

    <div id="primaryContent" class="container">
  	  <button  class="btn">添加</button>
	  <a href="/media/Warcraft III.zip">下载</a>
	  <form role="form" method="post"  action="/info">
	    <div class="form-group">
		  <label for="title">标题</label>
		  <input type="text" class="form-control" id="title" value=" value="中文" name="title"
		  placeholder="标题,请少于200字符">
	    </div>
	    <div class="form-group">
		  <label for="content">内容</label>
		  <textarea type="textarea" rows="20" class="form-control" id="content" name="content"

		  placeholder="填写内容,请少于2000字符">中文
				  </textarea >
	    </div>
	    <div class="form-group">
		  <label for="offdays">有效天数</label>
		  <input type="text" class="form-control" value="15" id="offdays" name="offdays"

	    </div>

	    <button type="submit" class="btn">发布</button>
	  </form>
      <!-- Example row of columns -->
      <div class="row">
        <div class="col-md-10">
        </div>
      </div>

      <%include ../footer.ejs%>
    </div>
    <!-- /container -->


      <%include ../scripts.ejs%>
      <script type="" src="/javascripts/build/info.js"> </script>
      <script>
        $(function(){
			$('form#frm_info').submit(function(){
				e.preventDefault();
			});	
				/*
            $('.blog').click(function(){
                var me = $(this);
                var title = me.text();
                $.get('blog/GoInstall',function(rlt){
			/** 
			var rlt = '<div style="background:#fff;"><span'+
				' class="glyphicon glyphicon-off"'+
				' style="position:absolute;right:5;"></span></div>' + rlt;
			**/
			var rlt = '<span class="glyphicon glyphicon-off"'+
				' style="position:absolute;right:5px;top:1px;"></span>' + rlt;
		
                    var div = $('.blog-content').html(rlt);
		    div.children('.glyphicon-off').click(function(){
			    div.html('');
		    });
                });
            });*/ 
        });
      </script>
  </body>
</html>
